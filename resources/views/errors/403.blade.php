{{-- 앱 곳곳의 abort(403, '구체적인 메시지')가 실제로 화면에 보여야 하므로(예: "비밀글은
    작성자와 관리자만 볼 수 있습니다."), 예외에 메시지가 있으면 그걸 우선 쓰고 없을 때만
    일반적인 문구로 대체한다. --}}
@include('errors._page', [
    'code' => 403,
    'title' => __('접근 권한이 없습니다'),
    'message' => ($exception->getMessage() ?: null) ?? __('이 페이지에 접근할 수 있는 권한이 없습니다.'),
])
